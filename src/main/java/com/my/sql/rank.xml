<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.my.rank.RankMapper">

  <select id="selectByMonth" resultType="RankDTO" parameterType="java.util.HashMap">

	SELECT * 
	FROM MEMBER_RANK_#{teamno}
	WHERE rank_date BETWEEN TO_DATE('#{date}', 'yyyy-MM-dd HH:mm:ss') AND
	LAST_DAY(TO_DATE('#{date}', 'yyyy-MM-dd HH:mm:ss'))
	ORDER BY RANK;
	
  </select>
  
</mapper>
