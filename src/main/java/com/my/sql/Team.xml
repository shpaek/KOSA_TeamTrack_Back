<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.my.team.TeamMapper">
	<select id="selectAll" resultType="com.my.team.dto.Team">
		SELECT * FROM TEAM
	</select>
	<select id="selectByTeamNo" resultType="com.my.team.dto.Team">
		SELECT *, (S)
	</select>
	<select id="selectByTeamName" resultType="com.my.team.dto.Team">
		SELECT team_name, brief_info, viewcnt
		FROM TEAM
		WHERE team_name = #{aaa};
	</select>
	<!-- 확인 필요 -->
	<select id="selectByHashtag" resultType="com.my.team.dto.Team">
		SELECT team_name, study_type, onoffline, join_member, max_member,
		startdate, enddate, brief_info,
		(select LISTAGG(hashtag_name, ',') from TEAM_HASHTAG b where b.team_no =
		a.team_no)
		AS hashtags, viewcnt
		FROM TEAM a
		WHERE team_no IN
		(SELECT team_no FROM TEAM_HASHTAG
		WHERE hashtag_name LIKE '%#{aaa}%');
	</select>


	<insert id="insert" parameterType="Team">
		INSERT INTO customer(id, pwd, name)
		VALUES (#{id}, #{pwd}, #{name})
	</insert>
</mapper>